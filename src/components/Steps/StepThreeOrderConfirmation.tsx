import React, { memo } from 'react';
import { Box } from '@material-ui/core';

const StepThreeOrderConfirmation = () => {
  return (
    <Box>

    </Box>
  );
};

export default memo(StepThreeOrderConfirmation);